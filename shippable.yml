language: node_js
#build_image: shippableimages/ubuntu1204_python
node_js:
  - 0.10
before_install:
  - ~/.nvm/nvm.sh && && nvm install v4.1.2
  - node --version
  - npm install -g grunt-cli

install:
  - npm install

script:
  - 'true'
